# Repository Connectivity

This workspace is now configured to track the canonical GitHub repository.

```bash
git remote add origin https://github.com/KitBohdan/Aletheia-Core.git
```

Use `git fetch origin` to synchronize changes and `git push origin work` to share updates.
